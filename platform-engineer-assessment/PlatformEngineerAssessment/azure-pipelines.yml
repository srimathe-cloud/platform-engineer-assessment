trigger:
  branches:
    include:
      - main

pool:
  vmImage: 'ubuntu-latest'

variables:
  buildConfiguration: 'Release'

steps:

- checkout: self

# Install .NET SDK
- task: UseDotNet@2
  inputs:
    packageType: 'sdk'
    version: '8.x'

# Restore API
- script: |
    dotnet restore PlatformEngineerAssessment/PlatformEngineer.Api/PlatformEngineer.Api.csproj
  displayName: 'Restore API'

# Restore Worker
- script: |
    dotnet restore PlatformEngineerAssessment/PlatformEngineer.Worker/PlatformEngineer.Worker.csproj
  displayName: 'Restore Worker'

# Build API
- script: |
    dotnet build PlatformEngineerAssessment/PlatformEngineer.Api/PlatformEngineer.Api.csproj \
      --configuration $(buildConfiguration) \
      --no-restore
  displayName: 'Build API'

# Build Worker
- script: |
    dotnet build PlatformEngineerAssessment/PlatformEngineer.Worker/PlatformEngineer.Worker.csproj \
      --configuration $(buildConfiguration) \
      --no-restore
  displayName: 'Build Worker'

# Publish API
- script: |
    dotnet publish PlatformEngineerAssessment/PlatformEngineer.Api/PlatformEngineer.Api.csproj \
      -c $(buildConfiguration) \
      -o $(Build.ArtifactStagingDirectory)/api
  displayName: 'Publish API'

# Publish Worker
- script: |
    dotnet publish PlatformEngineerAssessment/PlatformEngineer.Worker/PlatformEngineer.Worker.csproj \
      -c $(buildConfiguration) \
      -o $(Build.ArtifactStagingDirectory)/worker
  displayName: 'Publish Worker'

# Docker Compose Build
- script: |
    cd PlatformEngineerAssessment
    docker compose build
  displayName: 'Docker Compose Build'

# Start Containers
- script: |
    cd PlatformEngineerAssessment
    docker compose up -d
    docker ps
  displayName: 'Run Containers'

# Publish Artifacts
- task: PublishBuildArtifacts@1
  inputs:
    pathToPublish: '$(Build.ArtifactStagingDirectory)'
    artifactName: 'drop'
    publishLocation: 'Container'